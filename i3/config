# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

#Windows
# Win <-> Alt implement: /etc/udev/hwdb.d/10-my-modifiers.hwdb
set $mod Mod4
# Alt
#set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8


# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
#bindsym $mod+Return exec --no-startup-id 'alacritty -e "tmux"'
bindsym $mod+Shift+Return exec --no-startup-id "WINIT_UNIX_BACKEND=x11 alacritty"
bindsym $mod+Return exec --no-startup-id "WINIT_UNIX_BACKEND=x11 alacritty -o shell.program=/bin/tmux"
#bindsym $mod+Return exec --no-startup-id kitty

#bindsym $mod+b exec --no-startup-id "cd ~/.config/gtk-3.0; [ $(date +%H) -lt 6 ] \
#			&& ln -sf ./settings.dark.ini ./settings.ini \
#			||ln -sf ./settings.light.ini ./settings.ini ;\
#			chromium --proxy-server='127.0.0.1:8118'"
bindsym $mod+b exec --no-startup-id "export http{s,}_proxy=http://localhost:8118; [ $(date +%H) -lt 6 -a $(date +H) -gt 19 ] \
			&& ln -sf ~/.config/gtk-3.0/settings.dark.ini ~/.config/gtk-3.0/settings.ini \
			||ln -sf ~/.config/gtk-3.0/settings.light.ini ~/.config/gtk-3.0/settings.ini ;\
			firefox"
#bindsym $mod+b exec --no-startup-id "export http{s,}_proxy=socks5://127.0.0.1:8080; firefox"
#bindsym $mod+b exec --no-startup-id chromium --proxy-server='127.0.0.1:8118'

#bindsym $mod+b exec --no-startup-id "export http_proxy=http://127.0.0.1:8118 https_proxy=http://127.0.0.1:8118;\
#			firefox"

#bindsym $mod+b exec --no-startup-id chromium
#bindsym $mod+b exec --no-startup-id chromium --proxy-server="192.168.101.1:8119"
#bindsym $mod+m exec --no-startup-id netease-cloud-music
#bindsym $mod+z exec --no-startup-id zathura
#bindsym $mod+s exec --no-startup-id simplescreenrecorder

#bindsym $mod+k exec nautilus

bindsym $mod+k exec --no-startup-id fcitx

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id "export LANG=zh_CN.utf-8; i3-dmenu-desktop --dmenu='rofi -show drun'"
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu='rofi -show drun'
bindsym $mod+d exec --no-startup-id "rofi -show drun"


# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+n focus down
bindsym $mod+e focus up
bindsym $mod+i focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+n move down
bindsym $mod+Shift+e move up
bindsym $mod+Shift+i move right

bindsym $mod+ctrl+h move left; move left; move left; move left; move left
bindsym $mod+ctrl+n move down; move down; move down; move down; move down
bindsym $mod+ctrl+e move up; move up; move up; move up; move up
bindsym $mod+ctrl+i move right; move right; move right; move right; move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

## split in horizontal orientation
#bindsym $mod+Shift+v split h
#
## split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split

#隱藏和顯示窗口
bindsym $mod+minus move scratchpad
bindsym $mod+plus scratchpad show

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent
bindsym $mod+x focus child
#bindsym $mod+x [urgent="latest"] focus

# focus the child container
#bindsym $mod+d focus child

# BEGIN: i3-workspace-groups
set $exec_i3_groups exec --no-startup-id i3-workspace-groups
bindsym $mod+s exec --no-startup-id i3-switch-active-workspace-group
# Assign workspace to a group
bindsym $mod+Shift+s exec --no-startup-id i3-assign-workspace-to-group
# Select workspace to focus on
bindsym $mod+w exec --no-startup-id i3-focus-on-workspace
# Move the focused container to another workspace
bindsym $mod+Shift+w exec --no-startup-id i3-move-to-workspace
# Rename/renumber workspace. Uses Super+Alt+n
bindsym Mod1+Mod4+n exec --no-startup-id i3-rename-workspace

bindsym $mod+Tab workspace back_and_forth
bindsym $mod+1 $exec_i3_groups workspace-number 1
bindsym $mod+2 $exec_i3_groups workspace-number 2
bindsym $mod+3 $exec_i3_groups workspace-number 3
bindsym $mod+4 $exec_i3_groups workspace-number 4
bindsym $mod+5 $exec_i3_groups workspace-number 5
bindsym $mod+6 $exec_i3_groups workspace-number 6
bindsym $mod+7 $exec_i3_groups workspace-number 7
bindsym $mod+8 $exec_i3_groups workspace-number 8
bindsym $mod+9 $exec_i3_groups workspace-number 9
bindsym $mod+0 $exec_i3_groups workspace-number 10
#bindsym $mod+XF86AudioMute			$exec_i3_groups workspace-number 11
#bindsym $mod+XF86AudioLowerVolume 	$exec_i3_groups workspace-number 12
#bindsym $mod+XF86AudioRaiseVolume 	$exec_i3_groups workspace-number 13
#bindsym $mod+XF86AudioMicMute 		$exec_i3_groups workspace-number 14
#bindsym $mod+XF86MonBrightnessDown	$exec_i3_groups workspace-number 15
#bindsym $mod+XF86MonBrightnessUp	$exec_i3_groups workspace-number 16
## ...

bindsym $mod+Shift+1 exec --no-startup-id "i3-workspace-groups move-to-number  1 && i3-workspace-groups workspace-number  1"
bindsym $mod+Shift+2 exec --no-startup-id "i3-workspace-groups move-to-number  2 && i3-workspace-groups workspace-number  2"
bindsym $mod+Shift+3 exec --no-startup-id "i3-workspace-groups move-to-number  3 && i3-workspace-groups workspace-number  3"
bindsym $mod+Shift+4 exec --no-startup-id "i3-workspace-groups move-to-number  4 && i3-workspace-groups workspace-number  4"
bindsym $mod+Shift+5 exec --no-startup-id "i3-workspace-groups move-to-number  5 && i3-workspace-groups workspace-number  5"
bindsym $mod+Shift+6 exec --no-startup-id "i3-workspace-groups move-to-number  6 && i3-workspace-groups workspace-number  6"
bindsym $mod+Shift+7 exec --no-startup-id "i3-workspace-groups move-to-number  7 && i3-workspace-groups workspace-number  7"
bindsym $mod+Shift+8 exec --no-startup-id "i3-workspace-groups move-to-number  8 && i3-workspace-groups workspace-number  8"
bindsym $mod+Shift+9 exec --no-startup-id "i3-workspace-groups move-to-number  9 && i3-workspace-groups workspace-number  9"
bindsym $mod+Shift+0 exec --no-startup-id "i3-workspace-groups move-to-number 10 && i3-workspace-groups workspace-number 10"
# END: i3-workspace-groups

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

## i3lock
#mode "i3lock: Return to lock/Escape to Cancel" {
#            bindsym Return mode "default" exec --no-startup-id ~/.config/i3/scripts/i3lock/i3lock.sh
#            bindsym Escape mode "default"
#    }
#bindsym $mod+ctrl+l mode "i3lock: Return to lock/Escape to Cancel"

#mode "Shutdown the machine now?" {
#            bindsym Return mode "default" exec --no-startup-id "alacritty -e /usr/bin/shutdown -h now"
#            bindsym Escape mode "default"
#    }
#bindsym $mod+shift+s mode "Shutdown the machine now?"
#mode "You pressed the exit shortcut. Do you really want to exit i3? This will end your X session." {
#            bindsym Return mode "default" exec --no-startup-id i3-msg exit
#            bindsym Escape mode "default"
#    }
#bindsym $mod+shift+f mode "You pressed the exit shortcut. Do you really want to exit i3? This will end your X session."


#===power manager===
#https://blog.megumifox.com/public/2019/08/07/i3wm-%E9%85%8D%E7%BD%AE%E5%83%9E/
set $mode_system select: lock(L) exit(E) reboot(R) poweroff(O) suspend(S) hybrid-sleep(Y) hibernate(H) cancel(Esc)
bindsym $mod+Shift+f mode "$mode_system"
mode "$mode_system" {
#bindsym l exec --no-startup-id i3lock -n -t --ignore-empty-password --no-unlock-indicator  -i background.png  , mode "default"
	bindsym l exec --no-startup-id ~/.config/i3/scripts/i3lock/i3lock.sh, mode "default"
	bindsym e exec --no-startup-id i3-msg exit, mode "default"
	bindsym s exec --no-startup-id systemctl suspend, mode "default"
	bindsym y exec --no-startup-id systemctl hybrid-sleep, mode "default"
	bindsym h exec --no-startup-id systemctl hibernate, mode "default"
	bindsym r exec --no-startup-id systemctl reboot, mode "default"
	bindsym o exec --no-startup-id systemctl poweroff, mode "default"
	bindsym Escape mode "default"
}
bindsym $mod+l exec --no-startup-id "~/.config/i3/scripts/i3lock/i3lock.sh -f|xargs notify-send"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.

        bindsym h resize shrink width 1 px or 1 ppt
        bindsym n resize grow height 1 px or 1 ppt
        bindsym e resize shrink height 1 px or 1 ppt
        bindsym i resize grow width 1 px or 1 ppt

        bindsym shift+h resize shrink width 50 px or 50 ppt
        bindsym shift+n resize grow height 50 px or 50 ppt
        bindsym shift+e resize shrink height 50 px or 50 ppt
        bindsym shift+i resize grow width 50 px or 50 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Up resize grow height 10 px or 10 ppt
        bindsym Down resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# sound control
#alsamixer
bindsym XF86AudioRaiseVolume exec --no-startup-id "amixer set Master playback 5%+;\
	canberra-gtk-play -i audio-volume-change -d 'changeVolume';\
	notify-send 'volume up' $( amixer |grep -o -E '[0-9][0-9]?[0-9]?%' -m 1 ) -t 700 -h int:transient:1 "

bindsym XF86AudioLowerVolume exec --no-startup-id "amixer set Master playback 5%-;\
	canberra-gtk-play -i audio-volume-change -d 'changeVolume';\
	notify-send 'volume down' $( amixer |grep -o -E '[0-9][0-9]?[0-9]?%' -m 1 ) -t 700 -h int:transient:1 "

bindsym XF86AudioMute exec --no-startup-id "~/.config/i3/scripts/mute.sh"

bindsym --release XF86AudioLowerVolume exec pkill -SIGRTMIN+10 i3blocks
bindsym --release XF86AudioMute        exec pkill -SIGRTMIN+10 i3blocks
bindsym --release XF86AudioRaiseVolume exec pkill -SIGRTMIN+10 i3blocks

#Audio
bindsym XF86AudioMicMute exec --no-startup-id "~/.config/i3/scripts/microphone.sh"
#bindsym XF86AudioMicMute exec "amixer set Capture toggle"
#bindsym --release XF86AudioMicMute exec "bash sleep 0.2; pkill -SIGRTMIN+11 i3blocks"

# screen brightness
#bindsym XF86MonBrightnessUp exec --no-startup-id "xbacklight -d $DISPLAY -inc 6; notify-send 'brightness' 'up' -t 700 -h int:transient:1"
#bindsym XF86MonBrightnessDown exec --no-startup-id "xbacklight -d $DISPLAY -dec 6; notify-send 'brightness' 'down' -t 700 -h int:transient:1"
bindsym XF86MonBrightnessUp exec --no-startup-id "brightnessctl -d 'intel_backlight' set +6%>/dev/null; notify-send 'brightness' 'up' -t 700 -h int:transient:1"
bindsym XF86MonBrightnessDown exec --no-startup-id "brightnessctl -d 'intel_backlight' set 6%- >/dev/null; notify-send 'brightness' 'down' -t 700 -h int:transient:1"
bindsym XF86Display exec --no-startup-id " ~/.config/i3/scripts/multiscreen.sh"
bindsym XF86KbdBrightnessDown exec --no-startup-id "brightnessctl --device='smc::kbd_backlight' set 6%-"
bindsym XF86KbdBrightnessUp exec --no-startup-id   "brightnessctl --device='smc::kbd_backlight' set +6%"


## screenshot scrot
#bindsym --release Print exec --no-startup-id "scrot -q 100 -z -s '/home/ben/Pictures/screenshots/%Y-%m-%d_%H%M%S_$wx$h_scrot.png'"
##-e 'mv $f ~/Pictures/screenshots'"
#bindsym --release ctrl+Print exec --no-startup-id "scrot -q 100 -z -bs '/home/ben/Pictures/screenshots/%Y-%m-%d_%H%M%S_$wx$h_scrot.png'"
##-e 'mv $f ~/Pictures/screenshots'"
#bindsym shift+Print exec --no-startup-id "scrot -q 100 -z '/home/ben/Pictures/screenshots/%Y-%m-%d_%H%M%S_$wx$h_scrot.png' -e 'xclip -selection clipboard -t image/png -i $f && rm $f'"
##-e 'mv $f ~/Pictures/screenshots'"

# screenshot scrot
bindsym --release Print exec --no-startup-id "~/.config/i3/scripts/screenshot.sh 'select'"
#-e 'mv $f ~/Pictures/screenshots'"
bindsym --release ctrl+Print exec --no-startup-id "~/.config/i3/scripts/screenshot.sh 'border'"
#-e 'mv $f ~/Pictures/screenshots'"
bindsym shift+Print exec --no-startup-id "~/.config/i3/scripts/screenshot.sh 'default'"
bindsym $mod+Print exec --no-startup-id "flameshot gui"
##-e 'mv $f ~/Pictures/screenshots'"

#Bluetooth
bindsym XF86Bluetooth exec --no-startup-id "alacritty -o window.{dimensions.{columns=30,lines=6},position.{x=50,y=50}} --title BLUETOOTH -e ~/.config/i3/scripts/bluetooth.sh"
#bindsym XF86Bluetooth exec --no-startup-id "~/.config/i3/scripts/bluetooth"

#Hotspot
#bindsym XF86Favorites exec --no-startup-id "alacritty -o window.{dimensions.{columns=30,lines=6},position.{x=50,y=50}} --title HOTSPOT -e ~/.config/i3/scripts/hotspot.sh"
bindsym XF86Favorites exec --no-startup-id "~/.config/i3/scripts/hotspot"

#keyboard
#bindsym XF86 exec "~/.config/i3/Keyboard/Keyboard.sh""
#
# Calculator
#bindsym XF86Calculator exec --no-startup-id "alacritty -o window.{dimensions.{columns=60,lines=10},position.{x=50,y=50}} --title CALCULATOR -e ~/.config/i3/scripts/calculator.sh"
bindsym XF86Calculator exec --no-startup-id "env LANG='zh_CN.UTF-8' qalculate-qt"




bar {
    strip_workspace_numbers yes
#i3bar_command i3bar
#status_command i3status -c ~/.config/i3/i3status.conf
        status_command i3blocks -c ~/.config/i3blocks/config

        height 20
        position top
        font pango: SauceCodePro Nerd Font, Icons 9
#font pango: FontAwesome, Icons 9
        colors {
            active_workspace #333333 #333333 #ffffff
                background #222222
                focused_workspace #0088CC #0088CC #ffffff
                inactive_workspace #333333 #333333 #888888
                separator #666666
                statusline #dddddd
                urgent_workspace #2f343a #900000 #ffffff
        }
}

exec_always --no-startup-id i3-autoname-workspaces

#exec --no-startup-id ~/.config/i3/inverse-scroll.sh
#exec_always sleep 1; xmodmap ~/.xmodmap_colemak

# head title
default_border none
# edge
default_floating_border normal
# You can also use any non-zero value if you'd like to have a border
#for_window [class=".*"] border pixel 0


# https://github.com/manzerbredes/i3-colors
client.focused          #0088CC #0088CC #ffffff #dddddd #0088CC
client.focused_inactive #333333 #333333 #888888 #292d2e #333333
client.unfocused        #333333 #333333 #888888 #292d2e #333333
client.urgent           #2f343a #900000 #ffffff #900000 #2f343a

#for_window [class="Chromium"] floating enable
exec_always --no-startup-id "python ~/.config/i3/scripts/border_for_floating.py"
#focus_follows_mouse no
# Set inner/outer gaps
gaps inner 17
gaps outer -2
# Only enable gaps on a workspace when there is at least one container
smart_gaps on


#exec_always sleep 1.5; screenkey

# exec ss-qt5

# # exec --no-startup-id ~/.config/i3/scripts/tap-to-click.sh
# # scroll inverse

bindsym $mod+p exec --no-startup-id variety -n
bindsym $mod+shift+p exec --no-startup-id variety -p
# vim ma mark
bindsym $mod+m exec --no-startup-id "variety -f && notify-send 'Added' -t 700 -h int:transient:1"

bindsym $mod+shift+t exec --no-startup-id "~/.config/i3/scripts/translate.sh clipboard"
bindsym $mod+t exec --no-startup-id " ~/.config/i3/scripts/translate.sh selection"



#exec ss-qt5
# replaced with xorg.conf.d/40-libinput.conf: Option "NaturalScrolling" "true"
#exec --no-startup-id ~/.config/i3/scripts/inverse-scroll.sh
#exec --no-startup-id fcitx5
# press ~ to claim a new workspace
#bindsym $mod+grave exec --no-startup-id ~/.config/i3/scripts/i3-new-workspace

#exec --no-startup-id sleep 5;xmodmap ~/.config/i3/xmodmap_colemak
#exec --no-startup-id sleep 5;xmodmap ~/.config/i3/xmodmap_winalt

# compton
#exec --no-startup-id picom --config ~/.config/i3/picom.conf
#exec --no-startup-id variety

#notification
#exec --no-startup-id dunst

#exec --no-startup-id xset s 0 -dpms

#exec --no-startup-id xautolock -corners -0+0 -cornersize 30 -cornerdelay 2 -time 5 -locker "source ~/.config/i3/scripts/player_screen.sh && ( xset s 7 -b; i3lock-fancy -n; xset s off )" -detectsleep -bell 0
#bindsym $mod+Shift+l exec --no-startup-id "xset s 7 -b; i3lock -n; xset s off"
#
#exec_always --no-startup-id killall xautolock;xautolock -corners -0+0 -cornersize 30 -cornerdelay 2 -time 5 -locker "~/.config/i3/scripts/i3lock/i3lock.sh" -detectsleep -bell 0
exec --no-startup-id xautolock -corners -0+0 -cornersize 30 -cornerdelay 2 -time 5 -locker "~/.config/i3/scripts/i3lock/i3lock.sh" -detectsleep -bell 0
bindsym $mod+Shift+l exec --no-startup-id "~/.config/i3/scripts/i3lock/i3lock.sh"
#exec --no-startup-id ~/.config/i3/scripts/i3lock/phone_lock.sh
# detect activity:
#xprintidle
#exec --no-startup-id "autotiling --debug >> ~/autoliling.log"
exec_always --no-startup-id "pgrep autotiling 2>&1 || autotiling"

exec --no-startup-id "WINIT_UNIX_BACKEND=x11 alacritty -o shell.program=/bin/tmux"
#exec --no-startup-id "kitty"
exec --no-startup-id "~/.config/i3/scripts/boot_with_only_secondary_screen.sh"



# get the name of a window by `xprop`
for_window [class="wechat.exe"] floating enable
#for_window [title="HOTSPOT"] floating enable
for_window [title="BLUETOOTH"] floating enable
for_window [title="CALCULATOR"] floating enable
for_window [class="qalculate-qt"] floating enable
for_window [title="OkularEditor"] floating enable
for_window [class="tim.exe"] floating enable
for_window [class="wemeetapp.exe"] floating enable
for_window [class="wemeetapp"] floating enable


# 固定部分軟體出現的容器
#assign [class="VirtualBox"] 0
for_window [class="netease-cloud-music"] sticky enable pixel 0
